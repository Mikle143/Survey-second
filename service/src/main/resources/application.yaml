#db:
#  username:postgres
#  password:postgres
#  driver:PostgresDriver
#  url:jdbc:postgresql://localhost:5432/survey_2
#  hosts:localhost,127.0.0.1

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/survey_2
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
